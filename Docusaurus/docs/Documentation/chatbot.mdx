---
id: chatbot
title: Chatbot
sidebar_label: Chatbot
---

## Ask Questions

You can use the chatbox below to ask questions about Streamlit.

<button id="runStreamlitAppBtn">Run Streamlit App</button>

<script>
document.getElementById("runStreamlitAppBtn").onclick = function() {
    // Execute code to run the Streamlit app on the server
    fetch('http://localhost:5000/run_streamlit_app').then(function(response) {
        return response.text();
    }).then(function(result) {
        console.log(result);
    });
};
</script>

